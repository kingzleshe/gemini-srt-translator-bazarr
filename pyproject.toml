[project]
name = "gemini-srt-translator-bazarr"
version = "0.0.0"
description = "Bazarr companion worker for gemini-srt-translator"
requires-python = ">=3.12,<3.15"
dependencies = [
    "gemini-srt-translator>=3.6.2,<4",
]

[tool.uv]
package = false
